import { counterReducer } from './model/slices/counterSlice';
import { TCounterSchema } from './model/types/counterSchema';
import { Counter } from './ui/counter';

export { TCounterSchema, Counter, counterReducer };
